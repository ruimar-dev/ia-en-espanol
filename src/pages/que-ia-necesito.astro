---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="¿Qué IA necesito? — Test interactivo | IA en Español"
  description="Responde 5 preguntas y descubre qué herramienta de inteligencia artificial es perfecta para ti. Quiz gratuito y personalizado."
>
  <!-- INTRO -->
  <section id="intro" class="relative overflow-hidden py-24 px-4 text-center">
    <div class="absolute inset-0 -z-10">
      <div class="absolute -top-40 left-1/2 -translate-x-1/2 w-[900px] h-[700px] bg-[#7C3AED]/8 dark:bg-[#7C3AED]/5 rounded-full blur-3xl"></div>
    </div>
    <div class="max-w-2xl mx-auto">
      <span class="inline-block bg-[#7C3AED]/10 text-[#7C3AED] text-sm font-semibold px-4 py-1.5 rounded-full mb-6">
        🤖 Test interactivo
      </span>
      <h1 class="text-5xl sm:text-6xl font-bold text-gray-900 dark:text-white mb-4 leading-tight tracking-tight">
        ¿Qué IA<br/>
        <span class="text-[#7C3AED]">necesitas?</span>
      </h1>
      <p class="text-lg text-gray-600 dark:text-gray-400 mb-10 max-w-lg mx-auto leading-relaxed">
        5 preguntas rápidas y te decimos exactamente qué herramienta de IA usar. Sin tecnicismos, sin rodeos.
      </p>
      <div class="flex flex-wrap justify-center gap-x-6 gap-y-2 mb-10 text-sm text-gray-500 dark:text-gray-400">
        <span class="flex items-center gap-1.5"><span>⚡</span> Solo 5 preguntas</span>
        <span class="flex items-center gap-1.5"><span>🎯</span> Recomendación personalizada</span>
        <span class="flex items-center gap-1.5"><span>🆓</span> Gratis, sin registro</span>
      </div>
      <button
        id="btn-start"
        class="inline-flex items-center gap-2 bg-[#7C3AED] hover:bg-[#6D28D9] text-white font-bold px-8 py-4 rounded-2xl text-lg transition-all duration-200 hover:scale-105 shadow-xl shadow-[#7C3AED]/30"
      >
        Empezar el test
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
        </svg>
      </button>
    </div>
  </section>

  <!-- QUIZ -->
  <section id="quiz-section" class="hidden py-8 pb-24 px-4">
    <div class="max-w-3xl mx-auto">
      <!-- Progress -->
      <div class="mb-8">
        <div class="flex justify-between items-center mb-2">
          <span id="progress-label" class="text-sm font-medium text-gray-600 dark:text-gray-400">Pregunta 1 de 5</span>
          <span id="progress-pct" class="text-sm font-semibold text-[#7C3AED]">0%</span>
        </div>
        <div class="h-2 bg-gray-200 dark:bg-gray-800 rounded-full overflow-hidden">
          <div id="progress-bar" class="h-full bg-[#7C3AED] rounded-full transition-all duration-500" style="width: 0%"></div>
        </div>
        <div class="flex gap-1.5 mt-3">
          <div id="dot-1" class="h-1.5 flex-1 rounded-full bg-gray-200 dark:bg-gray-800 transition-all duration-300"></div>
          <div id="dot-2" class="h-1.5 flex-1 rounded-full bg-gray-200 dark:bg-gray-800 transition-all duration-300"></div>
          <div id="dot-3" class="h-1.5 flex-1 rounded-full bg-gray-200 dark:bg-gray-800 transition-all duration-300"></div>
          <div id="dot-4" class="h-1.5 flex-1 rounded-full bg-gray-200 dark:bg-gray-800 transition-all duration-300"></div>
          <div id="dot-5" class="h-1.5 flex-1 rounded-full bg-gray-200 dark:bg-gray-800 transition-all duration-300"></div>
        </div>
      </div>

      <!-- Question card -->
      <div id="question-card" class="bg-white dark:bg-[#1A1A1A] rounded-3xl border border-gray-200 dark:border-gray-800 p-6 sm:p-10 shadow-xl">
        <p id="q-numero" class="text-xs font-bold uppercase tracking-widest text-[#7C3AED] mb-3">Pregunta 1</p>
        <h2 id="q-texto" class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-1 leading-snug"></h2>
        <p id="q-subtitulo" class="text-sm text-gray-500 dark:text-gray-400 mb-8"></p>
        <div id="opciones-grid" class="grid gap-3"></div>
      </div>

      <!-- Email capture card -->
      <div id="email-card" class="hidden bg-white dark:bg-[#1A1A1A] rounded-3xl border border-gray-200 dark:border-gray-800 p-6 sm:p-10 shadow-xl text-center">
        <div class="text-6xl mb-4">🎉</div>
        <p class="text-xs font-bold uppercase tracking-widest text-[#7C3AED] mb-2">¡Ya casi!</p>
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-3 leading-snug">
          Tu resultado te espera
        </h2>
        <p class="text-gray-500 dark:text-gray-400 mb-8 max-w-xs mx-auto leading-relaxed text-sm">
          Déjanos tu email y te enviamos las mejores reviews de herramientas IA cada semana. Sin spam, solo lo bueno.
        </p>

        <form id="email-form" novalidate class="space-y-3 max-w-xs mx-auto text-left">
          <div>
            <input
              id="email-input"
              type="email"
              placeholder="tu@email.com"
              autocomplete="email"
              class="w-full px-4 py-3 rounded-xl border-2 border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-white placeholder-gray-400 focus:outline-none focus:border-[#7C3AED] transition-colors text-sm"
            />
            <p id="email-error" class="hidden text-xs text-red-500 mt-1 ml-1">Introduce un email válido.</p>
          </div>
          <button
            type="submit"
            id="btn-email-submit"
            class="w-full bg-[#7C3AED] hover:bg-[#6D28D9] text-white font-bold py-3 px-6 rounded-xl transition-all duration-200 hover:scale-[1.02] flex items-center justify-center gap-2 text-sm"
          >
            <span id="btn-email-text">Ver mi resultado</span>
            <svg id="btn-email-arrow" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
            </svg>
            <svg id="btn-email-spinner" class="hidden w-4 h-4 animate-spin" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"/>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8z"/>
            </svg>
          </button>
        </form>

        <button id="btn-skip-email" class="mt-5 text-xs text-gray-400 hover:text-gray-500 dark:hover:text-gray-300 transition-colors underline underline-offset-2">
          Saltar y ver el resultado
        </button>
      </div>
    </div>
  </section>

  <!-- RESULTS -->
  <section id="results-section" class="hidden py-8 pb-24 px-4">
    <div class="max-w-3xl mx-auto">
      <div class="text-center mb-10">
        <div id="confetti-container" class="relative h-16 mb-2 overflow-visible pointer-events-none"></div>
        <p class="text-xs font-bold uppercase tracking-widest text-[#7C3AED] mb-2">Tu resultado</p>
        <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-2">Tu IA ideal es...</h2>
        <p class="text-gray-600 dark:text-gray-400">Basado en tus respuestas, aquí está tu recomendación.</p>
      </div>

      <!-- Winner card -->
      <div id="winner-card" class="rounded-3xl p-6 sm:p-8 mb-6 text-white shadow-2xl overflow-hidden">
        <div class="flex items-start gap-4 mb-4">
          <div class="text-5xl flex-shrink-0" id="winner-emoji"></div>
          <div>
            <p class="text-sm font-semibold opacity-75 mb-0.5" id="winner-empresa"></p>
            <h3 class="text-3xl font-bold leading-none" id="winner-nombre"></h3>
          </div>
        </div>
        <p class="text-lg font-semibold opacity-90 mb-3" id="winner-tagline"></p>
        <p class="opacity-80 mb-5 leading-relaxed text-sm sm:text-base" id="winner-desc"></p>
        <div class="flex flex-wrap gap-2 mb-6" id="winner-pros"></div>
        <div class="flex flex-col sm:flex-row items-start sm:items-center gap-3">
          <a id="winner-link" href="#" target="_blank" rel="noopener noreferrer"
            class="inline-flex items-center gap-2 bg-white/20 hover:bg-white/30 border border-white/30 text-white font-semibold px-5 py-2.5 rounded-xl transition-colors text-sm">
            Probar ahora
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
            </svg>
          </a>
          <span class="flex items-center gap-1.5 text-sm opacity-75">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <span id="winner-precio"></span>
          </span>
        </div>
      </div>

      <!-- Alternatives -->
      <div class="mb-10">
        <h3 class="text-xs font-bold uppercase tracking-widest text-gray-500 dark:text-gray-400 mb-4">También puede interesarte</h3>
        <div id="alt-grid" class="grid grid-cols-1 sm:grid-cols-2 gap-4"></div>
      </div>

      <!-- Restart -->
      <div class="text-center pt-4 border-t border-gray-200 dark:border-gray-800">
        <button id="btn-restart" class="inline-flex items-center gap-2 text-[#7C3AED] hover:text-[#6D28D9] font-semibold transition-colors text-sm">
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
          </svg>
          Repetir el test
        </button>
      </div>
    </div>
  </section>
</Layout>

<style>
  @keyframes slideInRight {
    from { transform: translateX(60px); opacity: 0; }
    to   { transform: translateX(0);    opacity: 1; }
  }
  @keyframes slideOutLeft {
    from { transform: translateX(0);     opacity: 1; }
    to   { transform: translateX(-60px); opacity: 0; }
  }
  @keyframes fadeInUp {
    from { transform: translateY(40px); opacity: 0; }
    to   { transform: translateY(0);    opacity: 1; }
  }
  @keyframes popIn {
    0%   { transform: scale(1); }
    40%  { transform: scale(1.06); }
    100% { transform: scale(1); }
  }
  @keyframes confettiFall {
    0%   { transform: translateY(0)     rotate(0deg);   opacity: 1; }
    100% { transform: translateY(80px)  rotate(720deg); opacity: 0; }
  }

  .slide-in  { animation: slideInRight 0.35s cubic-bezier(0.25, 0.46, 0.45, 0.94) both; }
  .slide-out { animation: slideOutLeft 0.25s cubic-bezier(0.55, 0, 0.75, 0.06) both; }
  .fade-in-up { animation: fadeInUp 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94) both; }

  /* Los botones de opciones se crean en JS, por eso necesitan :global() */
  :global(.opcion-card) {
    cursor: pointer;
    border: 2px solid transparent;
    background: #F3F4F6;
    transition:
      transform    0.25s cubic-bezier(0.34, 1.56, 0.64, 1),
      box-shadow   0.2s  ease,
      border-color 0.15s ease,
      background   0.15s ease;
  }
  :global(.dark .opcion-card) {
    background: #111827;
  }
  :global(.op-emoji) {
    display: inline-block;
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  :global(.op-titulo) {
    transition: color 0.15s ease;
  }
  :global(.opcion-card:hover) {
    border-color: #7C3AED;
    transform: translateY(-4px) scale(1.02);
    box-shadow:
      0 0 0 4px rgba(124, 58, 237, 0.12),
      0 16px 32px rgba(124, 58, 237, 0.2);
    background: rgba(124, 58, 237, 0.04);
  }
  :global(.dark .opcion-card:hover) {
    background: rgba(124, 58, 237, 0.09);
  }
  :global(.opcion-card:hover .op-emoji) {
    transform: scale(1.3) rotate(-8deg);
  }
  :global(.opcion-card:hover .op-titulo) {
    color: #7C3AED;
  }
  :global(.opcion-card.selected) {
    border-color: #7C3AED;
    background: #7C3AED !important;
    animation: popIn 0.3s ease;
  }
  :global(.opcion-card.selected .op-titulo) { color: white; }
  :global(.opcion-card.selected .op-desc)   { color: rgba(255,255,255,0.75); }
  :global(.confetti-piece) {
    position: absolute;
    width: 9px;
    height: 9px;
    border-radius: 2px;
    animation: confettiFall 0.9s ease-out forwards;
  }

</style>

<script>
  interface Puntos { [key: string]: number }

  interface Opcion {
    emoji: string;
    titulo: string;
    desc: string;
    puntos: Puntos;
  }

  interface Pregunta {
    pregunta: string;
    subtitulo: string;
    grid: string;
    opciones: Opcion[];
  }

  interface IAInfo {
    nombre: string;
    empresa: string;
    emoji: string;
    tagline: string;
    descripcion: string;
    precio: string;
    web: string;
    pros: string[];
    colorFrom: string;
    colorTo: string;
  }

  const preguntas: Pregunta[] = [
    {
      pregunta: '¿Para qué usarás la IA principalmente?',
      subtitulo: 'Elige lo que más encaja con tu día a día',
      grid: 'grid-cols-2 sm:grid-cols-3',
      opciones: [
        { emoji: '✍️', titulo: 'Escribir y analizar', desc: 'Emails, resúmenes, documentos', puntos: { chatgpt: 3, claude: 3, gemini: 2, perplexity: 2, notion: 1 } },
        { emoji: '🎨', titulo: 'Crear imágenes', desc: 'Arte, diseño, ilustraciones', puntos: { midjourney: 3, firefly: 3, chatgpt: 1 } },
        { emoji: '💻', titulo: 'Programar', desc: 'Código, bugs, proyectos tech', puntos: { cursor: 3, copilot: 3, chatgpt: 2, claude: 1 } },
        { emoji: '🎙️', titulo: 'Audio y voces', desc: 'Podcasts, narración, voz IA', puntos: { elevenlabs: 3 } },
        { emoji: '🎬', titulo: 'Crear vídeos', desc: 'Generar o editar con IA', puntos: { runway: 3 } },
        { emoji: '📋', titulo: 'Organizar trabajo', desc: 'Notas, tareas, proyectos', puntos: { notion: 3, chatgpt: 2, gemini: 2, claude: 1 } },
      ],
    },
    {
      pregunta: '¿Cuánto quieres gastar al mes?',
      subtitulo: 'Sin juicios, todas las opciones son válidas',
      grid: 'grid-cols-1 sm:grid-cols-3',
      opciones: [
        { emoji: '🆓', titulo: 'Gratis, a poder ser', desc: 'Solo quiero probar o no tengo presupuesto', puntos: { gemini: 3, perplexity: 2, chatgpt: 1 } },
        { emoji: '💰', titulo: 'Hasta 20 € al mes', desc: 'Pago si de verdad lo merece', puntos: { chatgpt: 3, claude: 3, cursor: 2, copilot: 2, midjourney: 2, elevenlabs: 1 } },
        { emoji: '💎', titulo: 'Lo que haga falta', desc: 'Quiero lo mejor sin límites', puntos: { claude: 2, midjourney: 2, runway: 3, elevenlabs: 2, copilot: 1 } },
      ],
    },
    {
      pregunta: '¿Cómo te defines con la IA?',
      subtitulo: 'Sé honesto, no hay respuesta incorrecta',
      grid: 'grid-cols-1 sm:grid-cols-3',
      opciones: [
        { emoji: '🌱', titulo: 'Soy principiante', desc: 'Poca o ninguna experiencia aún', puntos: { chatgpt: 3, gemini: 2, notion: 2 } },
        { emoji: '🚀', titulo: 'Ya uso alguna IA', desc: 'Conozco lo básico, quiero más', puntos: { claude: 2, chatgpt: 2, midjourney: 1, perplexity: 1 } },
        { emoji: '🔬', titulo: 'Soy bastante técnico', desc: 'Me manejo bien con herramientas complejas', puntos: { cursor: 3, copilot: 2, claude: 2, perplexity: 1 } },
      ],
    },
    {
      pregunta: '¿Qué es lo más importante para ti?',
      subtitulo: 'Elige solo uno — el que más pese en tu decisión',
      grid: 'grid-cols-2',
      opciones: [
        { emoji: '⚡', titulo: 'Velocidad', desc: 'Que funcione ya y sin complicaciones', puntos: { gemini: 3, chatgpt: 2, perplexity: 2 } },
        { emoji: '🎯', titulo: 'Máxima calidad', desc: 'Prefiero lo mejor aunque lleve más tiempo', puntos: { claude: 3, midjourney: 2, chatgpt: 1 } },
        { emoji: '🌈', titulo: 'Creatividad', desc: 'Que me sorprenda e inspire', puntos: { midjourney: 3, firefly: 3, claude: 2 } },
        { emoji: '🔒', titulo: 'Privacidad', desc: 'Mis datos no deben entrenar modelos', puntos: { claude: 3, perplexity: 1 } },
      ],
    },
    {
      pregunta: '¿Cómo trabajas habitualmente?',
      subtitulo: 'Último paso, ya casi terminamos 🏁',
      grid: 'grid-cols-1 sm:grid-cols-2',
      opciones: [
        { emoji: '🙋', titulo: 'Solo o uso personal', desc: 'Freelance, hobby o proyectos propios', puntos: { cursor: 2, claude: 2, chatgpt: 1, midjourney: 1, perplexity: 1 } },
        { emoji: '👥', titulo: 'Con equipo o empresa', desc: 'Colaboro con otras personas', puntos: { copilot: 3, notion: 3, chatgpt: 2, gemini: 1 } },
      ],
    },
  ];

  const IAs: Record<string, IAInfo> = {
    chatgpt: {
      nombre: 'ChatGPT',
      empresa: 'OpenAI',
      emoji: '🤖',
      tagline: 'El más popular y versátil del mercado',
      descripcion: 'ChatGPT es la referencia mundial de la IA. Funciona para casi cualquier tarea: escribir, analizar, programar, responder preguntas... Si no sabes por dónde empezar, empieza aquí. Tiene una interfaz muy intuitiva y una comunidad enorme.',
      precio: 'Gratis · Pro desde $20/mes',
      web: 'https://chat.openai.com',
      pros: ['Muy fácil de usar', 'Sirve para casi todo', 'GPTs personalizados'],
      colorFrom: '#10B981',
      colorTo: '#0D9488',
    },
    claude: {
      nombre: 'Claude',
      empresa: 'Anthropic',
      emoji: '🧠',
      tagline: 'El mejor para análisis profundo y escritura de calidad',
      descripcion: 'Claude destaca por su razonamiento, sus textos excelentemente redactados y su capacidad para procesar documentos muy largos. Es también uno de los más cuidadosos con la privacidad y el más honesto cuando no sabe algo.',
      precio: 'Gratis · Pro desde $20/mes',
      web: 'https://claude.ai',
      pros: ['Textos de altísima calidad', 'Documentos muy largos', 'Privacidad y precisión'],
      colorFrom: '#F97316',
      colorTo: '#D97706',
    },
    gemini: {
      nombre: 'Gemini',
      empresa: 'Google',
      emoji: '⭐',
      tagline: 'La mejor opción gratuita con integración Google',
      descripcion: 'Gemini es la IA de Google y se integra perfectamente con Gmail, Docs, Drive y el resto del ecosistema. Su versión gratuita es muy potente, tiene acceso a internet en tiempo real y es la mejor elección si ya usas Google.',
      precio: 'Gratis · Advanced desde $19.99/mes',
      web: 'https://gemini.google.com',
      pros: ['Completamente gratis', 'Integración con Google', 'Acceso a internet real'],
      colorFrom: '#3B82F6',
      colorTo: '#4F46E5',
    },
    perplexity: {
      nombre: 'Perplexity',
      empresa: 'Perplexity AI',
      emoji: '🔍',
      tagline: 'El buscador IA que siempre cita sus fuentes',
      descripcion: 'Perplexity es como un Google mejorado con IA que te da respuestas directas y cita siempre sus fuentes. Perfecto si necesitas información actualizada, investigar cualquier tema o verificar datos en tiempo real.',
      precio: 'Gratis · Pro desde $20/mes',
      web: 'https://perplexity.ai',
      pros: ['Fuentes siempre verificadas', 'Info en tiempo real', 'Ideal para investigar'],
      colorFrom: '#14B8A6',
      colorTo: '#0891B2',
    },
    midjourney: {
      nombre: 'Midjourney',
      empresa: 'Midjourney',
      emoji: '🎨',
      tagline: 'La IA de imágenes con mayor calidad artística',
      descripcion: 'Midjourney genera las imágenes más impresionantes del mercado. Si buscas arte, ilustraciones o imágenes de alto impacto visual, es la opción de referencia entre diseñadores y creativos de todo el mundo.',
      precio: 'Desde $10/mes',
      web: 'https://midjourney.com',
      pros: ['Calidad artística sin igual', 'Comunidad enorme', 'Muy personalizable'],
      colorFrom: '#8B5CF6',
      colorTo: '#6D28D9',
    },
    firefly: {
      nombre: 'Adobe Firefly',
      empresa: 'Adobe',
      emoji: '🔥',
      tagline: 'Imágenes IA pensadas para diseñadores',
      descripcion: 'Adobe Firefly está diseñado para uso comercial seguro y se integra directamente en Photoshop, Illustrator y el resto de apps de Adobe. Ideal si ya trabajas en el ecosistema Creative Cloud.',
      precio: 'Gratis con créditos · desde €4.99/mes',
      web: 'https://firefly.adobe.com',
      pros: ['Uso comercial seguro', 'Integración Adobe', 'Muy intuitivo'],
      colorFrom: '#EF4444',
      colorTo: '#DB2777',
    },
    cursor: {
      nombre: 'Cursor',
      empresa: 'Anysphere',
      emoji: '⚡',
      tagline: 'El editor de código con IA más potente',
      descripcion: 'Cursor es VS Code reinventado con IA nativa. Entiende todo tu proyecto, no solo el archivo abierto. Ideal para desarrolladores individuales que quieren multiplicar su productividad de forma drástica.',
      precio: 'Gratis · Pro desde $20/mes',
      web: 'https://cursor.sh',
      pros: ['IA nativa en el editor', 'Entiende todo el proyecto', 'Extremadamente rápido'],
      colorFrom: '#7C3AED',
      colorTo: '#5B21B6',
    },
    copilot: {
      nombre: 'GitHub Copilot',
      empresa: 'GitHub / Microsoft',
      emoji: '🚀',
      tagline: 'El estándar en IA para equipos de desarrollo',
      descripcion: 'GitHub Copilot es la opción de referencia en empresas. Se integra con cualquier IDE, gestiona código en equipo y tiene herramientas de revisión y seguridad. El más maduro para entornos profesionales.',
      precio: '$10/mes individual · $19/mes empresa',
      web: 'https://github.com/features/copilot',
      pros: ['Integración con GitHub', 'Ideal para equipos', 'Muy fiable y seguro'],
      colorFrom: '#374151',
      colorTo: '#1F2937',
    },
    elevenlabs: {
      nombre: 'ElevenLabs',
      empresa: 'ElevenLabs',
      emoji: '🎙️',
      tagline: 'Las voces sintéticas más realistas del mercado',
      descripcion: 'ElevenLabs convierte texto en voz con una naturalidad que impresiona. Puedes clonar tu propia voz, crear personajes con voces únicas o doblar contenido a cualquier idioma con calidad profesional.',
      precio: 'Gratis · desde $5/mes',
      web: 'https://try.elevenlabs.io/5pqit62qinao',
      pros: ['Voces ultra-realistas', 'Clonado de voz propia', 'Multi-idioma'],
      colorFrom: '#EAB308',
      colorTo: '#EA580C',
    },
    runway: {
      nombre: 'Runway',
      empresa: 'Runway',
      emoji: '🎬',
      tagline: 'Genera y edita vídeos con IA profesional',
      descripcion: 'Runway es la IA de vídeo más avanzada del mercado. Genera vídeos desde texto, aplica efectos imposibles de conseguir manualmente y edita con herramientas impulsadas por IA de calidad cinematográfica.',
      precio: 'Gratis · desde $15/mes',
      web: 'https://runwayml.com',
      pros: ['Generación de vídeo top', 'Edición con IA', 'Calidad profesional'],
      colorFrom: '#EC4899',
      colorTo: '#E11D48',
    },
    notion: {
      nombre: 'Notion AI',
      empresa: 'Notion',
      emoji: '📋',
      tagline: 'La IA integrada en tu espacio de trabajo',
      descripcion: 'Notion AI vive dentro de tu workspace de Notion. Te ayuda a redactar notas, resumir reuniones, crear tareas y organizar información de equipo. Perfecta si ya usas Notion o quieres adoptar una sola herramienta para todo.',
      precio: 'Requiere Notion · +$10/mes',
      web: 'https://notion.so',
      pros: ['Integrado en tu workspace', 'Ideal para equipos', 'Organización potente'],
      colorFrom: '#475569',
      colorTo: '#1E293B',
    },
  };

  // ── Config ───────────────────────────────────────────────────────
  // Crea una cuenta gratuita en formspree.io y pon aquí tu ID de formulario
  const FORMSPREE_ID = 'xojzddkv';

  // ── State ────────────────────────────────────────────────────────
  let preguntaActual = 0;
  let respuestas: Puntos[] = [];
  let animating = false;

  // ── Helpers ──────────────────────────────────────────────────────
  const $ = (id: string) => document.getElementById(id) as HTMLElement;

  function show(id: string) { $(id).classList.remove('hidden'); }
  function hide(id: string) { $(id).classList.add('hidden'); }

  function updateProgress(idx: number) {
    const total = preguntas.length;
    const pct = Math.round((idx / total) * 100);
    ($('progress-bar') as HTMLElement).style.width = `${pct}%`;
    $('progress-label').textContent = `Pregunta ${idx + 1} de ${total}`;
    $('progress-pct').textContent = `${pct}%`;
    for (let i = 1; i <= total; i++) {
      const dot = $(`dot-${i}`);
      if (i <= idx) {
        dot.style.background = '#7C3AED';
      } else if (i === idx + 1) {
        dot.style.background = '#A78BFA';
      } else {
        dot.style.background = '';
      }
    }
  }

  function renderQuestion(idx: number) {
    const p = preguntas[idx];
    const card = $('question-card');

    $('q-numero').textContent = `Pregunta ${idx + 1}`;
    $('q-texto').textContent = p.pregunta;
    $('q-subtitulo').textContent = p.subtitulo;

    const grid = $('opciones-grid');
    grid.className = `grid gap-3 ${p.grid}`;
    grid.innerHTML = '';

    p.opciones.forEach((op) => {
      const btn = document.createElement('button');
      btn.className = 'opcion-card rounded-2xl p-4 text-left w-full';
      btn.innerHTML = `
        <div class="op-emoji text-3xl mb-2">${op.emoji}</div>
        <div class="op-titulo font-semibold text-gray-900 dark:text-white text-sm leading-snug">${op.titulo}</div>
        <div class="op-desc text-xs text-gray-500 dark:text-gray-400 mt-0.5 leading-snug">${op.desc}</div>
      `;
      btn.addEventListener('click', () => selectOption(btn, op.puntos));
      grid.appendChild(btn);
    });

    card.classList.remove('slide-out');
    void card.offsetWidth; // force reflow
    card.classList.add('slide-in');
    card.addEventListener('animationend', () => card.classList.remove('slide-in'), { once: true });
  }

  function selectOption(btn: HTMLButtonElement, puntos: Puntos) {
    if (animating) return;
    animating = true;

    // Disable all other buttons
    $('opciones-grid').querySelectorAll('button').forEach(b => (b as HTMLButtonElement).disabled = true);
    btn.classList.add('selected');
    respuestas[preguntaActual] = puntos;

    setTimeout(() => {
      const card = $('question-card');
      card.classList.add('slide-out');
      card.addEventListener('animationend', () => {
        preguntaActual++;
        if (preguntaActual < preguntas.length) {
          updateProgress(preguntaActual);
          renderQuestion(preguntaActual);
          animating = false;
        } else {
          showEmailCapture();
          animating = false;
        }
      }, { once: true });
    }, 420);
  }

  function showEmailCapture() {
    // Barra al 100%
    ($('progress-bar') as HTMLElement).style.width = '100%';
    $('progress-label').textContent = '¡Test completado!';
    $('progress-pct').textContent = '100%';
    for (let i = 1; i <= 5; i++) $(`dot-${i}`).style.background = '#7C3AED';

    // Si ya capturamos el email antes, vamos directo a resultados
    if (localStorage.getItem('quiz_email')) {
      hide('quiz-section');
      showResults();
      return;
    }

    // El question-card ya tiene 'slide-out' de selectOption — está fuera de pantalla.
    // Solo hay que ocultarlo en el DOM y mostrar el email-card.
    const qCard = $('question-card');
    qCard.classList.add('hidden');
    qCard.classList.remove('slide-out');

    const eCard = $('email-card');
    eCard.classList.remove('hidden');
    void eCard.offsetWidth; // fuerza reflow para que la animación arranque
    eCard.classList.add('fade-in-up');
    eCard.addEventListener('animationend', () => eCard.classList.remove('fade-in-up'), { once: true });
  }

  async function submitEmail(email: string) {
    localStorage.setItem('quiz_email', email);
    if (FORMSPREE_ID) {
      try {
        await fetch(`https://formspree.io/f/${FORMSPREE_ID}`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json', Accept: 'application/json' },
          body: JSON.stringify({ email, fuente: 'quiz-que-ia-necesito' }),
        });
      } catch (_) {}
    }
  }

  function goToResults() {
    hide('quiz-section');
    showResults();
  }

  function calcularScores(): [string, number][] {
    const scores: Record<string, number> = {};
    respuestas.forEach((puntos) => {
      Object.entries(puntos).forEach(([ia, pts]) => {
        scores[ia] = (scores[ia] ?? 0) + pts;
      });
    });
    return Object.entries(scores).sort(([, a], [, b]) => b - a);
  }

  function spawnConfetti() {
    const container = $('confetti-container');
    const colors = ['#7C3AED', '#A78BFA', '#F59E0B', '#10B981', '#EF4444', '#3B82F6', '#EC4899'];
    for (let i = 0; i < 28; i++) {
      const piece = document.createElement('div');
      piece.className = 'confetti-piece';
      piece.style.left = `${10 + Math.random() * 80}%`;
      piece.style.top = '0';
      piece.style.background = colors[Math.floor(Math.random() * colors.length)];
      piece.style.animationDelay = `${Math.random() * 0.6}s`;
      piece.style.animationDuration = `${0.7 + Math.random() * 0.7}s`;
      piece.style.borderRadius = Math.random() > 0.5 ? '50%' : '2px';
      container.appendChild(piece);
    }
  }

  function showResults() {
    const ranked = calcularScores();
    // Pick winner from known IAs
    const [winnerId] = ranked.find(([id]) => IAs[id]) ?? ranked[0];
    const winner = IAs[winnerId];
    if (!winner) return;

    // Winner card
    const wCard = $('winner-card');
    wCard.style.background = `linear-gradient(135deg, ${winner.colorFrom}, ${winner.colorTo})`;

    $('winner-emoji').textContent = winner.emoji;
    $('winner-empresa').textContent = winner.empresa;
    $('winner-nombre').textContent = winner.nombre;
    $('winner-tagline').textContent = winner.tagline;
    $('winner-desc').textContent = winner.descripcion;
    $('winner-precio').textContent = winner.precio;
    ($('winner-link') as HTMLAnchorElement).href = winner.web;

    $('winner-pros').innerHTML = winner.pros.map(p =>
      `<span class="inline-flex items-center gap-1.5 bg-white/20 text-white text-xs font-medium px-3 py-1.5 rounded-full">
        <svg class="w-3 h-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
        </svg>${p}</span>`
    ).join('');

    // Alternatives
    const alts = ranked.filter(([id]) => id !== winnerId && IAs[id]).slice(0, 2);
    $('alt-grid').innerHTML = alts.map(([id]) => {
      const ia = IAs[id];
      return `
        <a href="${ia.web}" target="_blank" rel="noopener noreferrer"
          class="group flex items-center gap-4 p-4 bg-white dark:bg-[#1A1A1A] rounded-2xl border border-gray-200 dark:border-gray-800 hover:border-[#7C3AED]/60 hover:shadow-lg hover:shadow-[#7C3AED]/10 transition-all duration-300">
          <span class="text-3xl flex-shrink-0">${ia.emoji}</span>
          <div class="min-w-0 flex-1">
            <p class="font-bold text-gray-900 dark:text-white text-sm">${ia.nombre}</p>
            <p class="text-xs text-gray-500 dark:text-gray-400 mt-0.5 truncate">${ia.tagline}</p>
            <p class="text-xs text-[#7C3AED] font-medium mt-1">${ia.precio}</p>
          </div>
          <svg class="w-4 h-4 text-gray-400 group-hover:text-[#7C3AED] flex-shrink-0 transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
          </svg>
        </a>`;
    }).join('');

    show('results-section');
    const rs = $('results-section');
    rs.classList.add('fade-in-up');
    rs.addEventListener('animationend', () => rs.classList.remove('fade-in-up'), { once: true });

    spawnConfetti();
  }

  function startQuiz() {
    hide('intro');
    show('quiz-section');
    preguntaActual = 0;
    respuestas = [];
    updateProgress(0);
    renderQuestion(0);
  }

  function restartQuiz() {
    hide('results-section');
    $('confetti-container').innerHTML = '';
    // Resetear email card y question card
    $('email-card').classList.add('hidden');
    const qCard = $('question-card');
    qCard.classList.remove('hidden', 'slide-out');
    startQuiz();
  }

  // ── Email form ────────────────────────────────────────────────────
  $('email-form').addEventListener('submit', async (e) => {
    e.preventDefault();
    const input = $('email-input') as HTMLInputElement;
    const error = $('email-error');

    if (!input.value || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(input.value)) {
      error.classList.remove('hidden');
      input.classList.add('border-red-400');
      return;
    }
    error.classList.add('hidden');
    input.classList.remove('border-red-400');

    const btn = $('btn-email-submit') as HTMLButtonElement;
    btn.disabled = true;
    $('btn-email-text').textContent = 'Enviando...';
    $('btn-email-arrow').classList.add('hidden');
    $('btn-email-spinner').classList.remove('hidden');

    await submitEmail(input.value);
    goToResults();
  });

  $('btn-skip-email').addEventListener('click', goToResults);

  $('btn-start').addEventListener('click', startQuiz);
  $('btn-restart').addEventListener('click', restartQuiz);
</script>

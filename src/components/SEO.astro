---
interface Props {
  title: string;
  description?: string;
  image?: string;
  article?: boolean;
  publishDate?: Date;
  dateModified?: Date;
  canonicalUrl?: string;
}

const {
  title,
  description = 'Reviews y comparativas de herramientas de inteligencia artificial en español.',
  image = '/og-default.png',
  article = false,
  publishDate,
  dateModified,
  canonicalUrl,
} = Astro.props;

const siteUrl = import.meta.env.SITE_URL || Astro.site?.toString() || 'https://iaenespanol.es';
const canonical = canonicalUrl ?? new URL(Astro.url.pathname, siteUrl).toString();
const ogImage = image.startsWith('http') ? image : new URL(image, siteUrl).toString();
const fullTitle = title.includes('IA en Español') ? title : `${title} | IA en Español`;

const jsonLd = article && publishDate
  ? JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'Article',
      headline: title,
      description,
      image: ogImage,
      datePublished: publishDate.toISOString(),
      dateModified: (dateModified ?? publishDate).toISOString(),
      inLanguage: 'es',
      url: canonical,
      mainEntityOfPage: { '@type': 'WebPage', '@id': canonical },
      author: {
        '@type': 'Person',
        name: 'Sergio Ruiz',
        url: `${siteUrl}/sobre-mi`,
        sameAs: [
          'https://www.linkedin.com/in/sergio-ruiz-martin-a1530b316',
          'https://github.com/ruimar-dev',
        ],
      },
      publisher: {
        '@type': 'Organization',
        name: 'IA en Español',
        url: siteUrl,
        logo: {
          '@type': 'ImageObject',
          url: `${siteUrl}/images/logo web.png`,
        },
      },
    })
  : null;
---

<title>{fullTitle}</title>
<meta name="description" content={description} />
<link rel="canonical" href={canonical} />

<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:image" content={ogImage} />
<meta property="og:url" content={canonical} />
<meta property="og:type" content={article ? 'article' : 'website'} />
<meta property="og:site_name" content="IA en Español" />
<meta property="og:locale" content="es_ES" />
{article && publishDate && (
  <meta property="article:published_time" content={publishDate.toISOString()} />
)}
{article && dateModified && (
  <meta property="article:modified_time" content={dateModified.toISOString()} />
)}

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImage} />

{jsonLd && <script type="application/ld+json" set:html={jsonLd} />}
